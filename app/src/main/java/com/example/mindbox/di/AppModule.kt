package com.example.mindbox.di

// All application-level singletons (MetadataExtractor, SyncScheduler, VoiceRecognitionManager,
// QueryParser, QueryRouter, StructuredQueryEngine, SemanticSearchEngine, EmbeddingModelManager)
// are annotated with @Singleton + @Inject constructor and are provided automatically by Hilt.
// No manual @Provides are needed for these classes; this module is intentionally empty.
